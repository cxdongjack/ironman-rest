import { chai } from 'meteor/practicalmeteor:chai';
import { $ } from 'meteor/jquery';


describe('Lists_show_page', function () {
  beforeEach(function () {
  });

  afterEach(function () {
  });

  it('renders correctly with simple data3 ', function () {
      chai.assert.equal(1, 1);
  });

  it('renders correctly with simple data4 ', function () {
      chai.assert.equal(1, 1);
  });
});
